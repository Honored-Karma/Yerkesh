[pytest]
asyncio_mode = auto
markers =
    slow: marks tests as slow (LLM API calls) — skip with -m "not slow"
    integration: marks integration tests requiring real services
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
