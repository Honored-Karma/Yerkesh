# ─── Core Bot ────────────────────────────────────────────────────────────────
aiogram==3.28.2
groq==0.13.1
pydantic-settings==2.14.1
python-dotenv==1.2.2

# ─── MCP ─────────────────────────────────────────────────────────────────────
mcp==1.27.1

# ─── Web / Webhook ───────────────────────────────────────────────────────────
fastapi==0.115.4
uvicorn==0.34.0
aiohttp==3.11.12

# ─── Redis ───────────────────────────────────────────────────────────────────
redis==5.2.1

# ─── HTTP клиент ─────────────────────────────────────────────────────────────
httpx==0.27.2

# ─── Логирование / Трассировка ───────────────────────────────────────────────
structlog==24.4.0
opentelemetry-api==1.33.1
opentelemetry-sdk==1.33.1
opentelemetry-exporter-otlp==1.33.1

# ─── Аудио (голосовые сообщения) ─────────────────────────────────────────────
ffmpeg-python==0.2.0

# ─── Excel-отчёты ────────────────────────────────────────────────────────────
openpyxl==3.1.5

# ─── Шифрование ──────────────────────────────────────────────────────────────
cryptography==44.0.3

# ─── Тестирование ────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.24.0
pytest-mock==3.14.0
hypothesis==6.135.2

asyncpg==0.29.0          # PostgreSQL async driver (RAG, context store)
psycopg2-binary==2.9.10  # PostgreSQL sync driver (MCP postgres_server.py)
pgvector==0.3.5           # pgvector Python client
tiktoken==0.9.0           # точный подсчёт токенов для context_store
locust==2.37.2            # нагрузочное тестирование
pytest-cov==6.0.0         # coverage report
aioresponses==0.7.8       # mock HTTP в тестах
pytest-socket==0.7.0      # запрет реальных сетевых запросов в тестах

# ─── Documents / RAG ─────────────────────────────────────────────────────────
pdfplumber==0.11.4       # извлечение текста из PDF (для /api/documents/upload)
pypdf==4.3.1             # fallback PDF парсер
fastembed==0.6.1         # эмбеддинги без Ollama (Railway / облако)
